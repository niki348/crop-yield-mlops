# Core ML
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0

# Visualisation
matplotlib>=3.7
seaborn>=0.12

# MLOps — Experiment tracking & registry (Phase 4, 5, 6)
mlflow>=2.10

# MLOps — Data versioning (Phase 2)
dvc>=3.0
dvc-gdrive>=3.0

# MLOps — Hyperparameter tuning (Phase 5)
optuna>=3.5

# MLOps — REST API serving (Phase 7)
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.0

# MLOps — Drift monitoring (Phase 9)
evidently>=0.4

# Utilities
pyyaml>=6.0
python-dotenv>=1.0
pytest>=7.4
httpx>=0.26

# Colab tunnelling for MLflow UI and FastAPI (Phases 4, 7)
pyngrok>=7.0
